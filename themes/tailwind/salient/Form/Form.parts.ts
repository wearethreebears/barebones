import type { Parts } from "@barebones/types/component";
import type { Form } from "@barebones/types/components/Form";

// Base styles

export default function FormLocal(): Form {
  const parts: Parts = {
    component: {},
  };

  return {
    parts,
  };
}
