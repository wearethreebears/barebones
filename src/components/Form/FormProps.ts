import type { PropType } from "vue";

export const error = {
  required: true,
  type: String as PropType<string>,
};

export const label = {
  required: true,
  type: String as PropType<string>,
};
export const name = {
  required: true,
  type: String as PropType<string>,
};
