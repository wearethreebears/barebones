import type { Parts } from '@jumpstart/types/component';

// Base Classes
const parts: Parts = {
  component: {
    style: {
      'default': ['font-sans'],
    },
    color: {
      'default': ['text-slate-500'],
    }
  }
}

export default parts;
